name: 'Project 04'

on:
  workflow_dispatch:
  push:
    paths:
      - projects/04/**

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: project04
        run: find projects/04 -name *.tst | xargs -t -L1 ./tools/CPUEmulator.sh
